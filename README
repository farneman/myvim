# Sync vim config
cd ~
git clone http://github.com/farneman/myvim.git ~/.vim
ln -s ~/.vim/vimrc ~/.vimrc
ln -s ~/.vim/gvimrc ~/.gvimrc
cd ~/.vim
git submodule update --init

# Upgrade submodules
cd ~/.vim/bundle && git pull
